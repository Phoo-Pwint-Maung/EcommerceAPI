(For Master Branch)
A simple RESTful API built to manage a product catalog. 
It allows users to perform basic CRUD operations 
such as retrieving, adding, updating, and deleting products.
(For addDocker Branch)
In this branch;
Database: PostgreSQL (Relational Database)
Containerization: Docker & Docker Compose
